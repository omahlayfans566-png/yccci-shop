/**
 * storage.ts — Backblaze B2 has been completely removed.
 * All file storage now uses Cloudinary (see cloudinary.ts).
 *
 * This file is kept as an empty stub so TypeScript does not
 * complain about any stale import that might exist during
 * the transition. All actual storage logic is in cloudinary.ts.
 */
export { };
