/**
 * WhatsApp order forwarding has been removed.
 * All order management is handled through the Admin Panel.
 */
export function buildOrderMessage(): string { return ''; }
export function whatsappLink(): string | null { return null; }
